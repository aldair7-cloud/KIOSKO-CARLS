name = "carlsjr-stripe-worker"
main = "worker.js"
compatibility_date = "2024-01-01"

# La clave secreta se añade con: wrangler secret put STRIPE_SECRET_KEY
# Nunca escribas la clave secreta aquí directamente
